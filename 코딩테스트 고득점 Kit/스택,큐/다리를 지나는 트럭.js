// 모든 트럭이 다리를 건너려면 최소 몇 초가 걸리는지
// 왜 7kg 트럭이 1~2초 걸리는지 이해가 안됨
// 혹시 트럭이 2대 들어가는 다리니까 2초 걸린다는 의미인건가? -> 맞는거같음
// 1초에 출발함

function arraySum(arr) {
  let sum = 0;
  for (let i of arr) {
    sum += i;
  }

  return i;
}

function solution(bridge_length, weight, truck_weights) {
  let cnt = 0;
  let fs = [];
  let ing = [];

  console.log(Math.max(truck_weights));
  // 다리를 지난 트럭이 다 건너면 반복문 종료
  while (fs.length !== truck_weights.length) {
    // 다리를 건너는 트럭 추가
    // 다리를 지난 트럭 조건
  }

  return cnt;
}
